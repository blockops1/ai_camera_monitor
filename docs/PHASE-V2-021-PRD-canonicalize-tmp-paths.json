{
  "prd_id": "PHASE-V2-021",
  "title": "canonicalize transient image paths — move /tmp writes to data/frames/<camera_id>/<alert_id>/",
  "author": "the operator",
  "status": "active",
  "phase": "v2-data-hygiene",
  "plan_section": "v2-storage-paths",
  "trigger": "operator directive 2026-09-09: 'tell me why the temporary directory is used in production? Is that a common thing to write things outside of the sub directories that the application runs in?' followed by 'Add the temporary cleanup as a separate follow up.'",
  "background": "v2 application code writes transient image files to /tmp in production paths, despite the canonical project storage at <PROJECT_ROOT>/data/frames/<camera_id>/. Four production /tmp writes exist after PRD-V2-019 lands: (1) listener/pipeline.py:_crop_paths() (lines 51-60) saves vision-payload crops as /tmp/_ga.png and /tmp/_gb.png; (2) listener/pipeline.py:_tiny_png() (line 64, post US-019g) writes /tmp/_empty.png sentinel; (3) listener/pipeline.py:121 passes output_dir='/tmp' to infra/gate.py:run_gate(), which writes pairwise_diff_<alert_id>.png when GATE_KEEP_DISK_ARTIFACTS=true; (4) listener/pipeline.py:141 has a literal /tmp/diff.png placeholder (this last one dies in US-019j but the others remain). The legacy reasoning was 'transient files go to /tmp' — a Unix convention for scratch space that does not apply to v2 for four reasons: (A) macOS does not auto-clean /tmp (unlike Linux), so pairwise_diff_<id>.png files accumulate forever in production; (B) /tmp is not backed up to NAS or iCloud, so a Mac mini failure loses alert artifacts; (C) /tmp is not in any privacy scope — sensitive vehicle plate/face data sitting there is a §11.176 private-data-externalization violation (private data belongs under <PROJECT_ROOT>/data/, with .gitignore boundaries); (D) multi-process coordination is fragile — two daemons writing to /tmp/_ga.png clobber each other, while data/frames/<cam>/<alert_id>/_ga.png is namespaced. The fix is to use the canonical alert-scoped path: data/frames/<camera_id>/<alert_id>/<file>.png. This path mirrors the existing ring-buffer layout (data/frames/<camera_id>/frame_NNN.png) but adds alert-scoped subdirectories for transient artifacts. The 'alert_id' is a UUID generated by listener/daemon.py:194 on every webhook accept, available throughout pipeline.run() via alert.get('id').",
  "shipping_definition": "All US-021 stories marked done; pytest 132/132 (or higher) green; tree clean; grep -nE '^/tmp/' listener/pipeline.py infra/gate.py telegram_formatter/ returns zero hits in production code (no /tmp writes from the alert pipeline); scripts/check_no_tmp_production_writes.py exits 0 on the production tree; one live alert has round-tripped through the new pipeline with all transient files written under data/frames/<camera_id>/<alert_id>/ and visible via `find data/frames -name '*.png' | head -20`.",
  "scope_exclusions": [
    "Moving the existing ring-buffer files (data/frames/<cam>/frame_NNN.png) into alert-scoped subdirectories is NOT in this PRD — those are sliding-window storage managed by infra/frame_capture.py and are out of scope. Only NEW writes from the alert pipeline (US-019f/g/h already moved to .png; US-021 moves them off /tmp) move to the alert-scoped path.",
    "Cleanup of pre-existing /tmp/* residue from before this PRD is NOT included — operators can run `rm /tmp/_ga.png /tmp/_gb.png /tmp/_empty.png /tmp/pairwise_diff_*.png` manually once the daemon has been restarted.",
    "Configuration of the canonical path via env var (e.g., V2_DATA_ROOT) is NOT in this PRD — the path is hard-coded as data/frames/<camera_id>/<alert_id>/ relative to the project root, matching the existing ring-buffer convention. Operators who run v2 from a non-standard location get to fix it themselves.",
    "Adding per-alert metadata sidecars (JSON metadata next to each PNG describing alert timestamp, camera coords, vision output) is NOT in this PRD — that's a future observability story.",
    "Migrating tests/test_*.py /tmp/* fixture paths to tmp_path is NOT in this PRD — tests legitimately use /tmp as pytest scratch space, the no-tmp scanner excludes the tests/ directory."
  ],
  "design_principles": [
    "Canonical path, always: data/frames/<camera_id>/<alert_id>/<file>.png. The alert_id is a UUID generated at webhook accept (listener/daemon.py:194). The camera_id is the matched canonical ID (OFS, OFG, OFP, etc.). The file is one of: pairwise_diff.png, crop_a.png, crop_b.png. The sentinel /tmp/_empty.png is special-cased: it lives at <PROJECT_ROOT>/data/_sentinels/_empty.png (shared across alerts, not alert-scoped).",
    "No /tmp writes from production code: any `Path('/tmp/...')` or open('/tmp/...', 'wb') in production source (infra/, listener/, vehicle_position/, telegram_formatter/) is a scanner violation. Tests legitimately use tmp_path or /tmp; both are excluded.",
    "Path construction via pathlib.Path, not string concat: `data_dir = PROJECT_ROOT / 'data' / 'frames' / camera_id / alert_id; data_dir.mkdir(parents=True, exist_ok=True)`. PROJECT_ROOT is computed once at module import via `Path(__file__).resolve().parent.parent` (one level up from listener/ or infra/).",
    "Reuse the existing pattern: the ring buffer at infra/frame_capture.py already uses `data/frames/<camera_id>/frame_NNN.png` — US-021 mirrors that pattern but with an alert_id subdirectory layer for per-alert artifacts.",
    "mkdir(parents=True, exist_ok=True) is the only race-free way to handle concurrent alerts: two alerts racing to create the same <camera>/<alert_id>/ directory must both succeed (exist_ok=True). Different alert_ids never collide; same alert_id never collides (idempotent).",
    "Cleanup is the operator's responsibility: this PRD does not add automatic pruning of data/frames/<cam>/<alert_id>/. Once alert artifacts are written, they stay until the operator's NAS sync / backup policy handles them. PRD-V2-020 already removed orphan modules; US-021 only adds structure, not retention."
  ],
  "stories": [
    {
      "id": "US-021a",
      "title": "migrate listener/pipeline.py:_crop_paths() and _tiny_png() to canonical data/frames/<cam>/<alert_id>/ paths",
      "status": "todo",
      "depends_on": [],
      "estimate_loc": 30,
      "files": [
        "listener/pipeline.py",
        "infra/paths.py",
        "tests/test_pipeline.py"
      ],
      "card_body_fallback": "**Story.** Migrate the three /tmp writes from listener/pipeline.py to canonical alert-scoped paths under <PROJECT_ROOT>/data/frames/<camera_id>/<alert_id>/. **Rewrites:** (1) Add a small helper module infra/paths.py — exports `data_dir_for(camera_id: str, alert_id: str) -> Path` that returns `<PROJECT_ROOT>/data/frames/<camera_id>/<alert_id>` (creates it via mkdir(parents=True, exist_ok=True)), and `PROJECT_ROOT: Path` constant computed via `Path(__file__).resolve().parent.parent`. (2) listener/pipeline.py:_crop_paths() (lines 51-60) — accept camera_id and alert_id parameters (currently implicit), compute `out_dir = data_dir_for(camera_id, alert_id)`, change `a_p, b_p = '/tmp/_ga.png', '/tmp/_gb.png'` to `a_p, b_p = str(out_dir / 'crop_a.png'), str(out_dir / 'crop_b.png')`. (3) listener/pipeline.py:_tiny_png() (line 64, post US-019g) — sentinel lives at `<PROJECT_ROOT>/data/_sentinels/_empty.png`, not /tmp/_empty.png. Add a sibling helper `empty_png_path() -> Path` in infra/paths.py that ensures the sentinel directory exists and returns the canonical sentinel path. Update the function name and call site. (4) The callers of _crop_paths at pipeline.py:137 — pass camera_id and alert_id explicitly. (5) Tests in tests/test_pipeline.py — update mocks to point at tmp_path / 'data' / 'frames' / camera / alert_id / 'crop_a.png' instead of /tmp/_ga.png. **Why first story (no deps):** it touches the function called immediately after run_gate, which is the most central /tmp-write site. Doing it first means US-021b can reuse infra/paths.py. **Why estimate 30 LOC:** helper module + 3 path changes + test updates.",
      "ac": [
        "AC1: grep -nE '/tmp/_ga|/tmp/_gb|/tmp/_empty' listener/pipeline.py returns zero hits (no /tmp paths in production code).",
        "AC2: new file infra/paths.py exists with PROJECT_ROOT constant + data_dir_for() + empty_png_path() helpers; functions create parent directories via mkdir(parents=True, exist_ok=True).",
        "AC3: pytest tests/test_pipeline.py passes with updated mocks pointing at tmp_path-based canonical paths; pytest 132/132 (or higher) green.",
        "AC4: when the daemon processes a live alert, the crops appear at `data/frames/<camera_id>/<alert_id>/crop_a.png` and `crop_b.png` — verified via `find data/frames -name 'crop_*.png' | tail -5` after a smoke alert.",
        "AC5: one commit, message 'refactor(v2): US-021a — migrate pipeline.py _crop_paths + sentinel to canonical data paths'."
      ],
      "rationale": "These crops are the most sensitive artifacts in the alert pipeline — they are what Qwen3-VL sees to identify vehicles and what gets sent to Telegram. Leaving them in /tmp is a §11.176 violation (private data outside the canonical scope) and a privacy leak on a stolen Mac.",
      "passes": false,
      "attempts": 0,
      "last_error": null,
      "commit_sha": null,
      "task_id": null
    },
    {
      "id": "US-021b",
      "title": "migrate infra/gate.py:run_gate() output_dir from /tmp to canonical data path; gate writes pairwise_diff_<alert_id>.png under data/frames/<camera_id>/<alert_id>/",
      "status": "todo",
      "depends_on": [
        "US-021a"
      ],
      "estimate_loc": 25,
      "files": [
        "listener/pipeline.py",
        "infra/gate.py",
        "tests/test_gate.py",
        "tests/conftest.py"
      ],
      "card_body_fallback": "**Story.** listener/pipeline.py:121 currently calls `run_gate(..., output_dir='/tmp')`. After US-019j promotes pairwise_diff_path to a required artifact, gate writes pairwise_diff_<alert_id>.png to /tmp — sensitive visualization data leaking outside the canonical scope. Migrate to the canonical path. **Rewrites:** (1) listener/pipeline.py:121 — replace `output_dir='/tmp'` with `output_dir=str(data_dir_for(camera_id, alert_id))` (imports data_dir_for from infra.paths added in US-021a). (2) infra/gate.py:_write_pairwise_diff_image() (line 244 area) — verify it uses the passed output_dir parameter (no hardcoded /tmp). Add a smoke assert at the top of _write_pairwise_diff_image: `assert '/tmp' not in str(output_dir), 'gate must not write to /tmp'`. (3) tests/test_gate.py + tests/conftest.py — update tests that mocked output_dir='/tmp' to use tmp_path / 'data' / 'frames' / camera_id / alert_id. Add a new test `test_gate_pairwise_diff_does_not_write_to_tmp` that runs gate.run() and asserts the resulting pairwise_diff_path is NOT under /tmp. **Why depends on US-021a:** the data_dir_for helper must exist before pipeline.py can compute the right output_dir for gate. **Why estimate 25 LOC:** one parameter change + one assertion in gate + test rewrites.",
      "ac": [
        "AC1: grep -nE 'output_dir=[\"\\']/tmp' listener/pipeline.py infra/gate.py returns zero hits — no /tmp path passed as output_dir.",
        "AC2: pytest tests/test_gate.py passes; the new test_gate_pairwise_diff_does_not_write_to_tmp passes; pytest 132/132 (or higher) green.",
        "AC3: when the daemon processes a live alert, the pairwise_diff image appears at `data/frames/<camera_id>/<alert_id>/pairwise_diff.png` — verified via `find data/frames -name 'pairwise_diff*.png' | tail -5` after a smoke alert.",
        "AC4: pytest tests/test_pipeline.py::test_pipeline_uses_canonical_path_for_gate — passing; asserts pipeline.py:121 passes a non-/tmp output_dir derived from data_dir_for().",
        "AC5: one commit, message 'refactor(v2): US-021b — gate pairwise_diff writes to canonical data path, not /tmp'."
      ],
      "rationale": "Pairwise diff is the visualization aid attached to TG#1 alerts. It's the highest-leverage privacy leak in the system — it's exactly the kind of 'reconstructed' content (subject bbox + overlays) that exposes identifying detail to anyone with access to /tmp.",
      "passes": false,
      "attempts": 0,
      "last_error": null,
      "commit_sha": null,
      "task_id": null
    },
    {
      "id": "US-021c",
      "title": "add no-tmp-writes regression scanner + Makefile + pre-push hook",
      "status": "todo",
      "depends_on": [
        "US-021a",
        "US-021b"
      ],
      "estimate_loc": 25,
      "files": [
        "scripts/check_no_tmp_production_writes.py",
        "tests/test_check_no_tmp_production_writes.py",
        "Makefile",
        ".git/hooks/pre-push"
      ],
      "card_body_fallback": "**Story.** With US-021a/b done, v2 production code should contain zero /tmp writes. Install a regression guard so future code can't reintroduce `Path('/tmp/...')` or `open('/tmp/...', 'wb')` patterns. (1) Write scripts/check_no_tmp_production_writes.py — a static scanner that walks ONLY production source files (infra/, listener/, vehicle_position/, telegram_formatter/) and flags any match against the regex `['\"]/tmp/[a-zA-Z_][a-zA-Z0-9_]*['\"]|open\\(['\"]/tmp/|Path\\(['\"]/tmp/`. Excludes tests/, models/, scripts/, docs/, archive/, data/, .venv/, .git/. Excludes docs/PHASE-V2-021-PRD-canonicalize-tmp-paths.json itself (which legitimately mentions /tmp in scope_exclusion prose). (2) Write tests/test_check_no_tmp_production_writes.py that asserts the scanner produces zero matches on the current production tree. (3) Wire into the Makefile — add `make check-no-tmp` target; chain it into `make check-privacy` (or add a `make check-all` that runs privacy, no-resize, no-jpeg, no-tmp in sequence). (4) Update .git/hooks/pre-push to run the new scanner. **Why depends on a and b:** the test must run AFTER both stories so it passes against the canonicalized tree. **Why estimate 25 LOC:** same pattern as the existing check_no_private_data.py, check_no_image_resize.py, check_no_jpeg.py scanners.",
      "ac": [
        "AC1: scripts/check_no_tmp_production_writes.py exists, runs cleanly, exits 0 on the current production tree, exits non-zero (with file:line output) on a synthetic offending file.",
        "AC2: tests/test_check_no_tmp_production_writes.py exists and passes — it asserts the scanner produces zero matches on the v2 production tree as it stands after US-021a/b.",
        "AC3: `make check-no-tmp` (or the chained target) runs the scanner and exits 0.",
        "AC4: pytest 132/132 (or higher) green after the change.",
        "AC5: one commit, message 'feat(v2): US-021c — no-tmp-writes scanner + Makefile + pre-push wiring'."
      ],
      "rationale": "Without a guard, /tmp can grow back the same way it grew the first time — a one-line `out_path = '/tmp/something.png'` quietly added in a future PR. The scanner makes the operator's 'no /tmp in production' invariant mechanically enforceable, matching the existing pattern for privacy, no-resize, and no-jpeg.",
      "passes": false,
      "attempts": 0,
      "last_error": null,
      "commit_sha": null,
      "task_id": null
    },
    {
      "id": "US-021d",
      "title": "operator-driven smoke test — verify a live alert round-trips with all transient files under data/frames/<cam>/<alert_id>/, not /tmp",
      "status": "todo",
      "depends_on": [
        "US-021a",
        "US-021b",
        "US-021c"
      ],
      "estimate_loc": 30,
      "files": [
        "scripts/smoke_alert_canonical_paths.py"
      ],
      "card_body_fallback": "**Story.** With US-021a/b/c done, prove end-to-end that a real alert produces all transient files (crop_a.png, crop_b.png, pairwise_diff.png) under the canonical data/frames/<camera_id>/<alert_id>/ path, NOT under /tmp. (1) Write scripts/smoke_alert_canonical_paths.py — a small standalone harness that POSTs a synthetic Reolink-style alert JSON to http://127.0.0.1:8090/alert for a working camera (OFS, OFG, or OFP), waits for the daemon to process it, then runs these assertions: (a) HTTP 200 response, (b) `find data/frames/<camera_id>/<alert_id>/ -name '*.png'` returns at least 3 PNG files (crop_a, crop_b, pairwise_diff), (c) `ls /tmp/_ga.png /tmp/_gb.png /tmp/_empty.png /tmp/pairwise_diff_*.png 2>&1` shows all four as 'No such file or directory' — nothing leaked to /tmp during the alert, (d) the operator receives a Telegram alert containing the canonical-path crops (lossless PNG, no JPEG distortion). (2) The script prints a summary report at the end listing every file written and its path. (3) The script exits 0 only if all four assertions pass. **Why depends on a, b, c:** all migrations + scanner must be in place. **Why estimate 30 LOC:** harness is small, the actual proof is reading the directory contents.",
      "ac": [
        "AC1: scripts/smoke_alert_canonical_paths.py exists and runs cleanly against the live daemon.",
        "AC2: a successful run produces: HTTP 200, ≥3 PNG files under data/frames/<camera_id>/<alert_id>/, zero /tmp residue, and a Telegram alert with PNG crops.",
        "AC3: the operator confirms visually that the received Telegram alert shows native-resolution PNG crops (no JPEG artifacts on plates/faces).",
        "AC4: pytest 132/132 (or higher) green; the daemon is still running; logs/daemon.log shows the smoke alert processed in <500ms.",
        "AC5: one commit, message 'feat(v2): US-021d — smoke test for canonical-path alert pipeline'."
      ],
      "rationale": "Unit tests prove the code works; this story proves the system works. The operator's explicit directive was 'make sure the system is actually working' before public-GitHub push — this is the canonical-path verification step that pairs with US-019e's native-resolution verification.",
      "passes": false,
      "attempts": 0,
      "last_error": null,
      "commit_sha": null,
      "task_id": null
    }
  ]
}
