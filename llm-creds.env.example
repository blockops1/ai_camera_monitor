# llm-creds.env — Vision + text LLM endpoint config
# v2 only uses VISION_LLM (vision-8B on :8080 for VM1+VM2). Text LLM stays
# configured but unused; kept for parity with current 3-server topology.
VISION_LLM_URL=
VISION_LLM_MODEL=
TEXT_LLM_URL=
TEXT_LLM_MODEL=
VISION_LLM_TOKEN=
TEXT_LLM_TOKEN=
