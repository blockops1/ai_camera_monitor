# telegram-creds.env — Telegram bot credential
# v2 loads these at startup. File mode 600. NEVER commit values.
TELEGRAM_BOT_TOKEN=
# Read at runtime by listener/daemon.py::_load_home_env() from ~/.env
TELEGRAM_HOME_CHAT_ID=
